# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Commands

```bash
npm run dev      # dev server (Turbopack) on :3000
npm run build    # production build — also runs the TypeScript check
npm run lint     # eslint (flat config)
npx tsc --noEmit # typecheck alone
```

```bash
npm test         # vitest, single run
npm run test:watch
npx vitest run src/lib/extraction   # one directory
```

Tests use **real file fixtures, not mocks** — [src/lib/extraction/fixtures.ts](src/lib/extraction/fixtures.ts) builds valid PDF bytes and pptx zips in memory, so the parsers are exercised against actual formats. Keep that approach for the Mode B CSV parser.

**Next.js 16 + React 19.** `AGENTS.md` is generated by `next dev` and warns that this version's APIs differ from older training data — the bundled docs in `node_modules/next/dist/docs/` are the authority. Already load-bearing here: `params`/`searchParams`/`cookies`/`headers` are async-only, `middleware.ts` is now `proxy.ts` (exporting `proxy`), and `react-hooks/set-state-in-effect` is an **error**, not a warning. That last rule is why `localStorage` is read through `useSyncExternalStore` in [src/lib/persisted-state.ts](src/lib/persisted-state.ts) rather than in an effect — follow that pattern instead of reintroducing effect-based prop/state syncing.

## Setup

Copy `.env.local.example` to `.env.local` and fill in the Firebase web config. Until that exists `isFirebaseConfigured` is false and the app renders a setup notice instead of crashing, so the shell stays browsable — keep that graceful path intact when adding Firebase-dependent features.

`firestore.rules` and `storage.rules` are written but not yet deployed to a project.

## Build status

**M1 (auth + app shell)** — done. Design tokens, UI primitives, Firebase auth, the shell, and a working settings page.

**M2 (upload + parsing)** — done. Dropzone → `POST /api/documents` → server-side extraction → Firestore. Library and dashboard list real documents.

Next is M3 (quiz generation, both modes).

## Storage: text-only, by decision

Firebase Storage now requires the Blaze plan, so this project **does not use it**. The ingest route extracts text and discards the original file; `/documents/{docId}/pages/{n}` holds the extracted text, one document per page (150 pages of text would exceed Firestore's 1 MiB per-document cap).

Consequences to keep in mind: there is no document preview and no re-extraction from an original, so regeneration (§2.6) reads the stored page text. `storage.rules` is kept for a future change of heart but is not in `firebase.json` and is not deployed. Don't add Storage calls without revisiting the billing decision.

**Deployment constraint:** Vercel caps serverless request bodies at 4.5 MB, below this app's 25 MB file limit. Deploying there needs either a host without that cap or a move to client-side extraction — which would forfeit the server-side page-count guarantee below.

## Architecture

- `src/app/(auth)/` holds signed-out routes; `src/app/(app)/` holds the shell layout plus every signed-in route.
- Providers nest in the root layout as **Theme → Auth → Toast**, consumed via `useTheme()` / `useAuth()` / `useToast()`.
- `AuthGuard` is a UX gate only. The security boundary is `firestore.rules` / `storage.rules` scoped to the owning `uid` — never move an authorization decision into the client guard.
- Theme resolution is deliberately split: an inline script in `<head>` stamps `data-theme` before first paint (no flash), and `ThemeProvider` syncs React's resolved value back out to the DOM. Change one and you must change the other.
- [src/lib/types.ts](src/lib/types.ts) is the typed mirror of the spec's §4 Firestore model — treat it as the schema contract.

## Product architecture (from the spec)

[reviewhere-spec.md](reviewhere-spec.md) is the source of truth; update it when decisions change. Reviewhere turns a student's PDF/PPTX (≤150 pages, hard limit) into a mixed quiz: multiple-choice interleaved with free-text identification questions. The pipeline is **upload → server-side extraction → question generation → quiz session → attempt/results → library**.

### Two generation modes, one schema

This is the central architectural constraint. Question generation has two independent paths that **both write to the same `/quizzes/{quizId}/questions` schema** (§4), so quiz-taking, scoring, and review code must never branch on how a quiz was created (`generationMode` is metadata only):

- **Mode A (automatic)** — chunk extracted text, call an OpenRouter free-tier model with a JSON-only structured-output prompt, validate/dedupe server-side. Free models are unreliable at strict JSON, so the contract is: strict system prompt → schema validation → **one** auto-retry with a stricter instruction → fall back to offering Mode B. Handle 429s gracefully rather than failing hard.
- **Mode B (manual CSV)** — a permanent feature, not a stopgap. The app generates a copy-ready prompt with the user's settings *and the extracted document text already embedded*, the user runs it through any LLM, then pastes/uploads the returned CSV. The contract is fixed (§2.2.1):

  ```
  type,question,choice_a,choice_b,choice_c,choice_d,correct_answer,accepted_answers,explanation,source_page
  ```

  RFC 4180 quoting; `identification` rows leave the choice columns blank; `accepted_answers` is pipe-separated. The importer validates **row-by-row and surfaces bad rows in a preview** — never silently drop them.

### Invariants worth preserving

- **Server-side enforcement.** The 150-page cap is applied in [src/app/api/documents/route.ts](src/app/api/documents/route.ts) against a page count the server derives by parsing the file itself. The client never supplies it, and Firestore rules make `/documents` server-write-only so it cannot be forged. Keep it that way.
- **Scan detection needs two signals.** A document is only rejected as image-only when it has no text *and* a near-zero average per page — the empty-page ratio alone falsely rejects slide decks made of short titles. There's a regression test for this.
- **Identification matching is exact-match-with-typo-tolerance only.** Normalize case/whitespace/punctuation, then Levenshtein against `correctAnswer` + `acceptedAnswers` (start: ≤1 for answers under 8 chars, ≤2 above). Explicitly *not* synonym or partial-credit matching — do not "improve" this into fuzzy semantic matching.
- **Every question carries `sourcePage` and an `explanation`**, both surfaced in the review screen.
- **Questions are interleaved by type by default**, not grouped, to mimic a real exam.
- **Failed generation must not lose the document** — it stays in the library with a "regenerate" action.
- **Single free tier, ~4–5 users.** No billing, plans, quotas, or upgrade UI. The only cap is a soft per-user daily Mode A limit as a code constant, for OpenRouter quota protection.
- Offline/PWA support is explicitly out of scope.

## Design system

The UI is Apple-inspired and §7 of the spec is prescriptive — read it before writing any component. Tokens live in [src/app/globals.css](src/app/globals.css) as a Tailwind 4 `@theme`; use the semantic utilities (`bg-surface`, `text-secondary`, `rounded-xl`, `text-title2`) rather than raw hex or arbitrary sizes, so light and dark stay in sync.

One deliberate deviation: light-mode `--color-accent` is `#0071e3`, not the spec's `#0A84FF`, which sits at ~3.0:1 on white and fails the AA contrast §6 requires. `#0A84FF` is retained for the gradient and for dark mode.

Accessibility is a hard requirement, not polish: WCAG 2.1 AA contrast, visible focus rings (2px accent, 2px offset — never removed), full keyboard navigation including quiz shortcuts (1–4 for MCQ, Enter to submit), and a reduced-motion mode that disables spring animations. The global `prefers-reduced-motion` block in `globals.css` handles CSS transitions; Motion components additionally check `useReducedMotion()`.

## Open questions (§10)

Unresolved in the spec — check with the user rather than deciding unilaterally: which free OpenRouter model to default to (availability shifts, so verify at build time), the final typo-tolerance thresholds, and whether CSV import allows re-uploading a corrected file or only inline row fixes.
